import Footer from "@/components/Footer";
import Navbar from "@/components/Navbar";
import { ArrowRight, CheckCircle2 } from "lucide-react";
import { GradientBackground, type GradientVariant } from "@/components/ui/gradient-background";

export default function PageShell({ eyebrow, title, intro, sections, gradient = "soft" }: { eyebrow: string; title: string; intro: string; gradient?: GradientVariant; sections: { title: string; body: string; items?: string[] }[] }) { return <><Navbar/><main id="main-content" className="inner-page"><section className="inner-hero"><GradientBackground variant={gradient} overlayOpacity={gradient === "dark" ? 0.18 : 0} /><div className="container"><div className="eyebrow">{eyebrow}</div><h1 className="display">{title}</h1><p className="body-copy">{intro}</p><a className="btn btn-primary" href="/contact">Talk to our team <ArrowRight size={16}/></a></div></section><section className="inner-sections"><div className="container">{sections.map((section, index) => <article className={`inner-row ${index % 2 ? "tint" : ""}`} key={section.title}><div><div className="eyebrow">0{index + 1}</div><h2 className="section-title">{section.title}</h2><p className="body-copy">{section.body}</p></div>{section.items && <ul className="feature-list">{section.items.map(item => <li key={item}><CheckCircle2 size={17} color="var(--blue)"/>{item}</li>)}</ul>}</article>)}</div></section></main><Footer/></>; }
